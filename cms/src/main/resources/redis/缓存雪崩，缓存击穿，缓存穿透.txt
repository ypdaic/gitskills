缓存雪崩：

    1：当多个key同时失效时，导致大量请求怼到数据库，解决方案：将key的缓存时间不要设置为一样，设置随机时间


缓存击穿：
    1: 当一个key失效后，有大量请求请求这个key，由于key不存在，导致请求怼到数据库，解决方案：对该key加锁

缓存穿透：
    1：当大量请求请求一个本身就不存在的key时，由于key不存在，导致请求怼到数据库，解决方案：加锁，并允许存null值
    2：当大量请求请求多个不存在的key时，由于key不存在，导致请求怼到数据库，此次加锁解决不了问题的，解决方案：布隆过滤器