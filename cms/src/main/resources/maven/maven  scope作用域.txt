scope 用来控制依赖对编译，测试，运行的classpath的关系

1：compile  默认编译依赖范围。被依赖项目在编译，测试，运行都参与。打包的时候需包含进去
2：test     被依赖项目仅仅参与测试相关工作，如测试代码的编译，执行
3：provided 被依赖项目运行期无效，打包的时候不用包进去，如servlet-api
4: runtime  被依赖项目无需参与项目的编译。例如：jdbc驱动