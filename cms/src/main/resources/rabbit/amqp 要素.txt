包括如下要素：

    生成者，消费者，消息

    信道 （一个线程对应一个信道，多个线程使用一个TCP连接）

    交换器，队列，绑定，路由建


    生产者发送消息到交换器，并通过路由键发送到指定的队列，队列需要和路由键绑定

    一个队列上有多个消费者，每条消息只能被一个消费者消费

    如果发送一个没有队列绑定的路由键的消息，该消息会被丢弃

    消息确定：消费者收到的每一条消息都必须进行确认（自动确认，手动确认）