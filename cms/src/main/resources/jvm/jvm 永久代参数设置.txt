jdk1.7以前：-XX:PermSize:     -XX:MaxPermSize:
jdk1.7以后：-XX:MetaspaceSize: -XX:MaxMetaspaceSize:


方法区也叫永久代，用于存储已经被虚拟机加载的类信息，常量，静态变量等数据，运行时常量池时方法区的一部分
用于存放编译期间生产的各种字面量和符号引用


jdk 1.8之前运行时常量池放在方法区，jdk1.8之后是放在堆中
jdk 1.8之前方法区还存在，jdk1.8之后方法区被修改为元空间，且不会被jvm所管理，其大小受置于物理内存的限制，不受jvm限制