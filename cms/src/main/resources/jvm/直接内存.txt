直接内存：不是虚拟机运行时数据取的一部分，也不是java虚拟机规范中定义的内存区域
         如果使用NIO，这块区域会被频繁使用，在java堆内可以使用directByteBuffer对象直接引用并操作
         这块内存不受java堆内存的限制，但是受本机内存的限制，可以通过MaxDirectMemorySize来设置
         （默认与堆内存大小一样），所以也会出现OOM