内部使用Entry<K,V> 保存数据，理论上TreeMap存放的数据没有上限，Entry<K,V> 内部会保存parent,left,right节点的引用，当存放第一个元素时，会将TreeMap的root
指向该Entry，再次存放元素时，会和root的key比较，如果大于就将root的right 指向新的Entry<K,V>，否则就将root的left 指向新的Entry<K,V> 然后将
新的Entry<K,V> 的parent指向root，最终形成一个树