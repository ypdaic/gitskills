PriorityBlockingQueue 按优先级排序的无界阻塞队列，虽然可以指定数组大小，但是会随着数据的增加进行扩容

内部使用数组保存数据，数组默认长度为11，当存放的数据大于数组的长度时就会进行扩容，数组长度小于64时是扩容一倍，
                      否则扩容50%

                      写入和读取使用同一个lock，每次写入或者读取都会重新排序数组，优先级低的排在前面

                      take,peek,poll 方法都是取数组下标为0的数据