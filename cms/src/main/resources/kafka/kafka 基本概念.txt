消息：和其他消息中间的消息一样是数据单元
批次：一组消息，且属于同一个主题和分区

主题：kafka使用主题对消息进行分类
分区：主题下有若干个分区，每个分区在硬盘上就是一个文件

生成者：消息生成者
消费者：消息消费者，一个消费者可以订阅多个主题
偏移量：记录消息的唯一性，在一个分区中是唯一的
消费群组：多个消费者构成一个群组，群组可以保证每个分区只能被一个消费者使用

broker:一个独立的kafka服务器叫broker
集群：多个broker组成一个集群

保留消息：kafka默认保留策略是要么保留一段时间（7天），要么保留一定大小（比如1个G）

