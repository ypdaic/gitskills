分区不变化，消费者变化的场景：在使用kafka客户端默认自动提交的情况下，kafka默认提交是基于时间来控制的，
                           默认5秒提交一次，如果客户端在拉去数据并处理，但还没来的及提交数据时，kafka
                           挂掉了，分区再均衡后，没有提交的数据就会被其他消费者重复消费，所以就需要手动
                           进行提交