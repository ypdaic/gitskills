·先分组，按范围进行分组比如0-1000的数据在group1中，避免数据迁移，然后对0-1000的数据进行hash分片，使数据均匀避免热点数据

比如这个组有3个库，每个库有3张表，每个表还是按范围分片，那么这个组一起就是9张表，然后使用id对9取模，0-2的定位db0,3-5的定位到db1,6-8定位到db1,
然后定位到具体的库后，再使用范围定位到具体的表中，如1-300 到table1,301-600放到table2,601-1000放到table3中