<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.daiyanping.springcloud.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.daiyanping.springcloud.entity.User">
    <result column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
    </resultMap>

    <select id="queryPage" resultMap="BaseResultMap">
        select
            *
        from
            user
        <where>
            enable = 1
            <if test="dto.username != null">
                username = #{dto.username}
            </if>
            <if test="dto.password != null">
                and password = #{dto.password}
            </if>
        </where>
    </select>

    <select id="queryCount" resultType="java.lang.Integer">
        select count(*) from user
        <where>
            <if test="dto.username != null">
                username = #{dto.username}
            </if>
            <if test="dto.password != null">
                and password = #{dto.password}
            </if>
        </where>
    </select>

    <select id="queryPageForExport" resultType="java.util.HashMap">
        select * from user
        <where>
            <if test="dto.username != null">
                username = #{dto.username}
            </if>
            <if test="dto.password != null">
                and password = #{dto.password}
            </if>
        </where>
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        select * from user
        <where>
            <if test="dto.username != null">
                username = #{dto.username}
            </if>
            <if test="dto.password != null">
                and password = #{dto.password}
            </if>
        </where>
    </select>

</mapper>
